---
title: Pipeline
description: Static engine PipelineGraph export, and how it differs from the display-only workflow preview v1 protocol.
---

The `pipeline` domain (`src/ragspine/pipeline/`) is RAGSpine's code-first answer to the one
thing graph frameworks (Dify, LangGraph) give you that a plain-Python engine otherwise
doesn't: a **visual diagram of the pipeline**. Instead of a graph DSL you author and then
hope matches reality, RAGSpine _derives_ a static `PipelineGraph` from the real wiring and
emits it as Mermaid, DOT, or JSON.

It is small, leaf-level, and strictly read-only: it imports nothing from the orchestrator
(all introspection is `getattr` duck-typing), so it can never perturb the system it
describes.

<Callout type="info">
  This engine-topology model is distinct from [workflow preview
  v1](/en/docs/guides/workflows#preview-protocol-v1). `PipelineGraph` describes assembled RAGSpine
  components and exports Mermaid/DOT/JSON. A workflow preview describes one Dify-compatible workflow
  with redacted nodes, edges, and geometry. Both are read-only; neither is an execution format.
</Callout>

## Layout

<Files>
  <Folder name="pipeline" defaultOpen>
    <File name="graph.py" />
    <File name="topology.py" />
  </Folder>
</Files>

`graph.py` is the zero-dependency value layer (frozen dataclasses + exporters);
`topology.py` holds the three builders that read live composition. The public API
re-exports six names: `Node`, `Edge`, `PipelineGraph`, `agent_topology`,
`retriever_topology`, `service_topology`.

## The value model

`graph.py` defines three frozen dataclasses:

<TypeTable
  type={{
    Node: {
      type: 'id, label, kind, domain=None, symbol=None',
      description:
        'A pipeline node. kind ∈ stage | store | external | gate | channel. symbol is a dotted import path used by the drift guard (None for conceptual nodes).',
    },
    Edge: {
      type: 'src, dst, label=None, kind="flow"',
      description:
        'A directed edge. kind ∈ flow | conditional | data. label e.g. "route=structured" / "hit" / "miss".',
    },
    PipelineGraph: {
      type: 'title, nodes: tuple[Node], edges: tuple[Edge]',
      description: 'An immutable graph plus its exporters.',
    },
  }}
/>

`PipelineGraph` carries three exporters and a combiner:

| Method                          | Returns         | Output                                                                                                            |
| ------------------------------- | --------------- | ----------------------------------------------------------------------------------------------------------------- |
| `to_mermaid(*, direction="TD")` | `str`           | A `flowchart` — node shape is chosen by `kind` (gate `{}`, store `[(...)]`, channel `([...])`, else a rectangle). |
| `to_dot()`                      | `str`           | A Graphviz `digraph` (`rankdir=TB`).                                                                              |
| `to_dict()`                     | `dict`          | JSON-round-trippable `{title, nodes[...], edges[...]}`.                                                           |
| `merge(other, *, group=None)`   | `PipelineGraph` | Dedupe nodes by id (first wins), keep all edges; optionally tag added nodes with `domain=group`.                  |

<Callout type="info">
  Exports are **deterministic and byte-identical** across runs (stable declared order). The `domain`
  grouping round-trips through `to_dict`, but in v1 `to_mermaid` / `to_dot` do not emit
  subgraph/cluster blocks.
</Callout>

## The three builders

`topology.py` derives a graph from live, duck-typed composition — a node appears only when
its component is actually wired.

<Cards>
  <Card
    title="agent_topology(*, narrative_retriever=None)"
    description="The full request flow: parse_intent → clarify_scope (consults SecurityGate via a data edge) → route diamond → structured / narrative / composite branches. The narrative nodes appear only when a narrative_retriever is injected."
  ></Card>
  <Card
    title="retriever_topology(retriever)"
    description="The HybridRetriever sub-pipeline: prefilter → BM25 [+vector] [+multi-query] → RRF → top_k. The vector node appears iff retriever.embedding_backend is set; the multi-query node iff query_rewriter is set. Rerank is deliberately not in this subgraph."
  ></Card>
  <Card
    title="service_topology(app)"
    description="The service topology: FAQ short-circuit upstream of the agent, plus the async ingestion path (routes → queue → jobs). Duck-typed on app.state.faq_cache and app.state.queue."
  ></Card>
</Cards>

<Callout type="info">
  `HybridRetriever.topology()` (in the [retrieval](/en/docs/guides/retrieval) domain) is a thin
  delegator into `retriever_topology` — so a configured retriever can render itself without the
  pipeline package importing the orchestrator.
</Callout>

## The CLI — `scripts/topology.py`

`scripts/topology.py` renders any of the three topologies offline and deterministically (it
builds default/mock assemblies — no Redis, no API key). Run it from the project root:

```bash
python scripts/topology.py                                  # agent → Mermaid → stdout
python scripts/topology.py --which retriever --of dot
python scripts/topology.py --of json --out docs/generated/topology.json
```

Flags:

- `--of {mermaid,dot,json}` — output format (default `mermaid`).
- `--which {agent,retriever,service}` — which topology (default `agent`).
- `--out PATH` — write to a file (creates parent dirs); omit to print to stdout. Writing into
  the git-ignored `docs/generated/` keeps regenerated diagrams diffing cleanly.

Under the hood the CLI builds each topology from a default/offline assembly: `retriever` →
`retriever_topology(HybridRetriever([]))` (a pure-BM25 skeleton over an empty corpus);
`service` → `create_app(...)` with `MockProvider` / `FakeQueue` / empty `FAQCache`, then
`service_topology(app)`; `agent` → `agent_topology(narrative_retriever=object())` (a
sentinel so the narrative/composite branches show up).

## Python API

```python
from ragspine.pipeline import agent_topology

graph = agent_topology(narrative_retriever=object())
print(graph.to_mermaid())   # Mermaid flowchart string
print(graph.to_dot())       # Graphviz digraph string

import json
print(json.dumps(graph.to_dict(), ensure_ascii=False, indent=2))
```

## See also

<Cards>
  <Card
    title="Agent"
    href="/en/docs/guides/agent"
    description="The request flow agent_topology renders."
  />
  <Card
    title="Retrieval"
    href="/en/docs/guides/retrieval"
    description="The HybridRetriever whose .topology() delegates here."
  />
  <Card
    title="Service"
    href="/en/docs/guides/service"
    description="The FastAPI app service_topology introspects."
  />
  <Card
    title="CLI & scripts"
    href="/en/docs/getting-started/cli-and-scripts"
    description="topology.py and the other repo-root scripts."
  />
  <Card
    title="Workflow preview"
    href="/en/docs/guides/workflows#preview-protocol-v1"
    description="The versioned, redacted graph projection rendered by the template website."
  />
</Cards>
